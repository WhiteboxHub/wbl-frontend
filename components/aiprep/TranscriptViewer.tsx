import type { Transcript, TranscriptEvidence } from "@/types/aiprep";

export function TranscriptViewer({ transcript, evidence }: { transcript: Transcript | null; evidence: TranscriptEvidence[] | null }) {
  return <section className="rounded-2xl border border-gray-200 bg-white p-5 dark:border-gray-800 dark:bg-gray-900"><h2 className="text-lg font-bold text-gray-900 dark:text-white">Transcript and evidence</h2>{transcript ? <p className="mt-4 max-h-64 overflow-y-auto whitespace-pre-wrap text-sm leading-7 text-gray-700 dark:text-gray-300">{transcript.transcript_text}</p> : <p className="mt-3 text-sm text-gray-500 dark:text-gray-400">Transcript is not available for this assessment.</p>}{evidence && evidence.length > 0 && <div className="mt-5 border-t border-gray-100 pt-4 dark:border-gray-800"><h3 className="text-sm font-bold text-gray-900 dark:text-white">Report evidence</h3><div className="mt-3 space-y-3">{evidence.map((item, index) => <article key={`${item.dimension}-${index}`} className="rounded-lg bg-gray-50 p-3 text-sm dark:bg-gray-800"><p className="font-medium text-gray-800 dark:text-gray-100">{item.quote}</p><p className="mt-1 text-xs text-gray-500">{item.dimension}{item.timestamp_s !== null ? ` · ${item.timestamp_s}s` : ""}</p><p className="mt-1 text-xs text-gray-600 dark:text-gray-300">{item.observation}</p></article>)}</div></div>}</section>;
}
